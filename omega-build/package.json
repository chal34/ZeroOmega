{
  "name": "omega-build",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {},
  "scripts": {
    "deps": "cd .. && pnpm install",
    "build": "cd .. && pnpm -r run build",
    "test": "cd .. && pnpm -r run test"
  }
}
